# Multi-Agent ADE 派工架構：Pluggable Master-Worker 實作手冊

本文件整合如何利用 **Model Context Protocol (MCP)** 打造一個「可抽換 (Pluggable)」的多智能體派工中心。
透過 **互動式 CLI 啟動介面** 與 **高度結構化的提示詞 (Robust Prompts)**，確保 Master Agent 能夠穩定且精準地編排工作流。

---

## 1. 可抽換式系統架構 (Pluggable Architecture)

**啟動階段 (User 選擇)**：MCP Server 啟動時，會跳出互動式選單，讓使用者勾選本次想開啟的 Worker。
**運行階段 (Agent 決策)**：Master Agent 透過 MCP 查詢當前有哪些 Worker 可用，並動態分派。

+-------------------------------------------------------------+  
|   【主控端 Master】 (支援 MCP 之客戶端, e.g. Claude Desktop) |  
+------------------------------+------------------------------+  
                               | (MCP Protocol)  
                               v  
               +---------------+---------------+  
               |      Local MCP Hub Server     | <--- (終端機互動式選單)
               |      (mcp_worker_hub.py)      |      User 勾選啟動哪些 Worker
               +---------------+---------------+  
                               | 
       +-----------------------+-----------------------+  
       |                       |                       |  
       v                       v                       v  
+---------------+      +---------------+      +---------------+
| Git Worktree  |      | 【已啟動工作端】|      |Docker Sandbox |
| (檔案隔離建置)|<---->| (例如只勾了 1,4)|----->| (安全驗證區)  |
+---------------+      | [1] Local LLM |      +---------------+
                       | [4] Antigravity|
                       +---------------+

---

## 2. 實作步驟：互動式 MCP Server (mcp_worker_hub.py)

### **步驟一：安裝相依套件**
```bash  
pip install "mcp[cli]" questionary
```

### **步驟二：建立 MCP Server 腳本**
這個版本在啟動時會阻擋並詢問 User，確定後才正式啟動 MCP Server。

```python  
import asyncio  
import subprocess  
import os
import uuid
import questionary
from mcp.server.fastmcp import FastMCP

# 1. 互動式選擇要啟動的 Worker
def setup_workers():
    choices = [
        questionary.Choice("Local LLM (Ollama 70B)", "local_70b"),
        questionary.Choice("Claude Code CLI", "claude_cli"),
        questionary.Choice("Codex CLI", "codex_cli"),
        questionary.Choice("Antigravity CLI", "antigravity_cli")
    ]
    selected = questionary.checkbox("請選擇本次派工中心要啟動哪些 Worker？", choices=choices).ask()
    if not selected:
        print("未選擇任何 Worker，程式結束。")
        exit(0)
    return selected

active_workers = setup_workers()
print(f"已啟動 Workers: {', '.join(active_workers)}")

mcp = FastMCP("Local-Agent-Hub")
job_status = {}

@mcp.tool()
async def get_active_workers() -> str:
    """Master 呼叫此工具得知目前有哪些 Worker 可用，不可派發給不在名單上的 Worker"""
    return f"Active Workers: {', '.join(active_workers)}"

@mcp.tool()
async def create_worktree(branch_name: str, path: str) -> str:
    cmd = f"git worktree add -b {branch_name} {path}"
    process = await asyncio.create_subprocess_shell(cmd, stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.PIPE)
    stdout, stderr = await process.communicate()
    if process.returncode != 0: return f"[Error]: {stderr.decode()}"
    return f"[Success]: Worktree created at {path}"

@mcp.tool()  
async def delegate_to_worker(prompt: str, worker_type: str, working_dir: str) -> str:  
    """非同步派發任務給 Worker。必須傳入 working_dir (Worktree 路徑)"""
    if worker_type not in active_workers:
        return f"[Reject]: Worker '{worker_type}' is disabled. Available: {active_workers}"

    job_id = str(uuid.uuid4())
    job_status[job_id] = f"Running on {worker_type}"
    
    async def run_task():
        # Router
        if worker_type == "local_70b": cmd = f"cd {working_dir} && ollama run qwen2.5-coder:70b '{prompt}'"
        elif worker_type == "claude_cli": cmd = f"cd {working_dir} && claude -p '{prompt}'"
        elif worker_type == "codex_cli": cmd = f"cd {working_dir} && codex --prompt '{prompt}'"
        elif worker_type == "antigravity_cli": cmd = f"cd {working_dir} && antigravity run '{prompt}'"

        process = await asyncio.create_subprocess_shell(cmd, stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.PIPE)
        stdout, stderr = await process.communicate()
        job_status[job_id] = f"Completed:\n{stdout.decode()}" if process.returncode == 0 else f"Failed:\n{stderr.decode()}"
            
        # 推播通知
        notify_cmd = f'powershell -Command "New-BurntToastNotification -Text \'{worker_type} 任務完成\'"'
        await asyncio.create_subprocess_shell(notify_cmd)

    asyncio.create_task(run_task())
    return f"[Job Started]: ID={job_id}. Worker is processing in background."

@mcp.tool()
async def check_job_status(job_id: str) -> str:
    return job_status.get(job_id, "[Error]: Job ID not found.")

@mcp.tool()
async def run_in_sandbox(command: str, worktree_path: str) -> str:
    abs_path = os.path.abspath(worktree_path)
    docker_cmd = f"docker run --rm -v {abs_path}:/app -w /app node:18-alpine {command}"
    process = await asyncio.create_subprocess_shell(docker_cmd, stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.PIPE)
    stdout, stderr = await process.communicate()
    return f"[Sandbox Passed]:\n{stdout.decode()}" if process.returncode == 0 else f"[Sandbox Failed]: {stderr.decode()}"

if __name__ == "__main__":  
    mcp.run(transport="stdio")
```

---

## 3. 強健的系統指示詞 (Robust System Prompts)

為了解決大模型容易幻覺或不按牌理出牌的問題，Master Agent 的 Prompt 必須採用**高度結構化 (XML標籤)** 與 **嚴格的 SOP 限制**。

請將以下 Prompt 置於 Custom Instructions：

```markdown  
# Multi-Agent Master Orchestrator 核心協議

你是本專案的中央架構師。你的唯一目標是：拆解任務、分派給 Worker，並嚴格驗證結果。
請你「絕對服從」以下 SOP，嚴禁跳過任何步驟。

## 第一階段：資源盤點與規劃
收到使用者任務後，你必須：
1. 呼叫 `get_active_workers` 確認可用的資源池。
2. 使用 `<plan>` 標籤輸出你的任務拆解圖表，明確指出哪個子任務要交給哪個 Worker。
   範例格式：
   <plan>
   [子任務 1] - 負責人: local_70b - 原因: 大量樣板生成
   [子任務 2] - 負責人: claude_cli - 原因: 高階邏輯重構
   </plan>

## 第二階段：隔離派工 SOP (嚴禁跳過)
針對每一個要派發出去的子任務，必須嚴格依序執行：
1. **建立隔離區**：呼叫 `create_worktree`，建立獨立的分支 (e.g. `worker-task-1`)。
2. **派發背景任務**：呼叫 `delegate_to_worker`，**必須**帶入上一步建立的 `working_dir`。取得 `Job ID`。
3. **報告進度**：告訴使用者任務已在背景執行，請使用者稍後提醒你檢查。
4. **狀態檢查**：收到使用者提醒或等待一段時間後，呼叫 `check_job_status`。

## 第三階段：沙盒驗證與合併
1. **安全測試**：Worker 宣告完成後，**嚴禁**直接猜測程式碼是否正確。必須呼叫 `run_in_sandbox` 對該 Worktree 執行測試腳本。
2. **打回重練**：如果測試 Failed，把 Error Log 包裝進 Prompt，重新 `delegate_to_worker` 請 Worker 修復。
3. **審查與合併**：測試 Passed 後，由你親自審視 Worktree 的修改，若確認無誤，才可透過外部指令或請使用者 Merge 回主線。
```
